# credentials for DB
DB_NAME = 'f1db'        # name of the db
DB_HOST = 'localhost'
DB_USER = 'root'
# password should be placed in the secrets.py file

# if a db with name DB_NAME already exists should it
# automatically be overwritten?
# DEFAULT = FALSE
DO_AUTO_OVERWRITE_DB = False

# the name of the file containing all the "create table" sql code
# should be placed in the SQL directory
CREATE_TABLES_SQL_FILE_NAME = 'ex2.sql'

# the name of the file containing all the "update" sql code
UPDATE_TABLES_SQL_FILE_NAME = 'update.sql'